export interface Contact {
  name: string;
  phone: string;
  email: string;
  address: string;
  id: number;
}

export interface NewContact {
  name: string;
  phone: string;
  email: string;
  address: string;
}
